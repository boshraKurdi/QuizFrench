export type TTopic = {
    id?: number,
    course_id?: number,
    title: string,
    title_ar: string,
    description: string,
    description_ar: string,
}
export type TTopicData = {
    data: {
        id?: number,
        course_id?: number,
        title: string,
        title_ar: string,
        description: string,
        description_ar: string,
    }
}